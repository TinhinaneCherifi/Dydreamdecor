{% extends 'base.html.twig' %}

{% block title %}Nos articles - Dydream Decor{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-md-3">
            <h6>Filtrer par catégorie</h6>
            {{ form(form) }}
        </div>
        <div class="col-md-9">
            <h3 class="text-center">Nos articles</h3>
            <hr>
            <div class="row product-container">
                {% for item in items %}
                    <div class="col-md-4">
                        {% include 'item/one_in_index.html.twig' %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
    <p class="float-right">
        <a href="#">Back to top</a>
    </p>

{% endblock %}
